<!DOCTYPE html>
<html>
  <head>
    <title><%=  full_title(yield(:title))  %></title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %> <%# render is a rails helper, looks for file in app/views/layouts/_shim.html.erb and insert results into the view %>
      <div class="container">      <%# leading underscores indicate partials %>
        
        <% flash.each do | messages_type, message | %>
          <%= content_tag(:div, message, class:"alert alert-#{message_type}") %>
                  <% # Returns an HTML block tag of type name surrounding the content. Add HTML attributes by passing an attributes hash to options. %>
            <% # :success is a symbol, but embedded Ruby automatically converts it to the string "success" %>
            <% # Bootstrap CSS supports styling for four such flash classes (success, info, warning, and danger) %>
        <% end %>
        
        <%= yield %>
        <%= render 'layouts/footer' %>
        <%= debug(params) if Rails.env.development? %>
      </div>
  </body>
</html>
